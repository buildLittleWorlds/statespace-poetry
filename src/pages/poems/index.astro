---
import Layout from "../../layouts/Layout.astro";
import { getCollection } from "astro:content";

const items = await getCollection("poems"); // or "essays" for the essays page
---

<Layout title="Poems">
  <!-- or "Essays" for the essays page -->
  <h1>Poems</h1>
  <!-- or "Essays" for the essays page -->
  <ul>
    {
      items.map((item) => (
        <li>
          <a href={`/poems/${item.slug}`}>{item.data.title}</a>
          {item.data.date && <span> - {item.data.date.toDateString()}</span>}
        </li>
      ))
    }
  </ul>
</Layout>
